{{ 'section-extras.css' | asset_url | stylesheet_tag }}
{{ 'component-button.css' | asset_url | stylesheet_tag }}
<section class="section"><div class="container"><div class="auth">
  <h1>{{ 'customer.reset.title' | t }}</h1>
  <p class="text-muted">{{ 'customer.reset.text' | t }}</p>
  {%- form 'reset_customer_password' -%}
    {%- render 'form-errors', form: form -%}
    {%- assign label = 'customer.password' | t -%}
    {%- render 'field', id: 'reset-password', name: 'customer[password]', label: label, type: 'password', autocomplete: 'new-password', required: true, minlength: 5, errors: form.errors, field: 'password' -%}
    {%- assign label = 'customer.password_confirm' | t -%}
    {%- render 'field', id: 'reset-confirm', name: 'customer[password_confirmation]', label: label, type: 'password', autocomplete: 'new-password', required: true, minlength: 5, errors: form.errors, field: 'password_confirmation' -%}
    <button type="submit" class="btn btn--full">{{ 'customer.reset.submit' | t }}</button>
  {%- endform -%}
</div></div></section>
{% schema %}
{
  "name": "t:sections.main_reset_password.name",
  "settings": []
}
{% endschema %}
